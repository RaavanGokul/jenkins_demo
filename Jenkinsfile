pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Jenkins Pipeline is working correctly 🚀"
                sh 'ls -l'
            }
        }
    }
}
